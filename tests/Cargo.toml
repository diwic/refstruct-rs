[package]
name = "tester"
version = "0.1.0"
authors = ["David Henningsson <diwic@ubuntu.com>"]
build = "build.rs"

[dependencies]
refbuilder = { path = "../refbuilder/" }

[build-dependencies]
refbuilder = { path = "../refbuilder/" }
