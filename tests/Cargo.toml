[package]
name = "tests"
version = "0.1.0"
authors = ["David Henningsson <diwic@ubuntu.com>"]
build = "build.rs"

[dependencies]
refstruct = { path = "../refstruct/" }

[build-dependencies]
refstruct = { path = "../refstruct/" }
